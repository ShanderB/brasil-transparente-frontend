name: Deploy to Test Environment

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Commit SHA or branch to deploy (default: develop)'
        required: false
        default: 'develop'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: test

    steps:
      # Checkout do código da ref selecionada
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref }}

      # Node (caso seja necessário para alguma futura etapa)
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'

      # Instalação do Google Cloud SDK no runner
      - name: Install Google Cloud SDK
        run: |
          sudo apt-get update -y
          sudo apt-get install -y apt-transport-https ca-certificates gnupg
          echo "deb https://packages.cloud.google.com/apt cloud-sdk main" | sudo tee /etc/apt/sources.list.d/google-cloud-sdk.list
          curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
          sudo apt-get update -y
          sudo apt-get install -y google-cloud-sdk

      # Ativação explícita da Service Account com a key vinda do secret
      - name: Activate service account
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          echo "${GCP_SA_KEY}" > /tmp/key.json
          gcloud auth activate-service-account --key-file=/tmp/key.json --project="${{ secrets.GCP_PROJECT }}"
          gcloud auth list
          gcloud config list

      # Build no Cloud Build
      - name: Build Docker image
        run: |
          gcloud builds submit \
            --project="${{ secrets.GCP_PROJECT }}" \
            --tag gcr.io/${{ secrets.GCP_PROJECT }}/frontend:${{ github.sha }}

      # Deploy no Cloud Run
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy frontend-test \
            --image gcr.io/${{ secrets.GCP_PROJECT }}/frontend:${{ github.sha }} \
            --region="${{ secrets.REGION }}" \
            --platform=managed \
            --allow-unauthenticated
